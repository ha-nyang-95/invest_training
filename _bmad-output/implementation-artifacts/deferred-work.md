# Deferred Work Log

Items intentionally deferred from code reviews and implementation. Each entry cites the source review and the reason for deferral. Revisit during the referenced downstream story or sprint retrospective.

## Deferred from: code review of 1-1-프로젝트-bootstrap-uv-monorepo-scaffold (2026-04-21)

- pytest-xdist filesystem-mutating regression tests — theoretical cross-worker race on injected fixture files in `tests/regression/test_import_linter_contracts.py`. `--dist=loadfile` mitigates within one file; `_` prefix on fixture file prevents `import athena.X` auto-load. Revisit when test corpus grows or when a flaky run reproduces.
- Hatch hook silent `"unknown-dev"` fallback has no warning — `packages/athena-core/hatch_build.py:48-50` catches `(FileNotFoundError, subprocess.SubprocessError, OSError)` and falls back silently. Add `self.app.display_warning` / stderr print in Story 1.3 (CI hardening) or Story 1.9 (observability base).
- `end-of-file-fixer` / `trailing-whitespace` auto-fix in CI — `.pre-commit-config.yaml` hooks auto-modify files and return exit 1, which can fail PRs on benign whitespace churn against `pre-commit run --all-files`. Revisit when Story 1.3 adds the full 7-stage CI gate.
- CI `fetch-depth: 0` + post-sync dirty check — `.github/workflows/ci.yml` has full-history checkout, but if a future `uv sync` ever touches tracked files the hatch hook stamps `-dirty` on clean-CI wheels. Add `git status --porcelain` guard in Story 1.3.
- mypy hook `additional_dependencies` incomplete for future packages — `.pre-commit-config.yaml` extended with `polars>=1` + `duckdb>=1` in Story 1.4 Task 1.7 (schemas.py / parquet_shard.py / parquet_reader.py / feature_query.py all pass `mypy --strict` under the hook). Still pending: `python-kis` (Story 4.1 broker adapter) and `uvloop` (orchestrator on Linux). Extend as each import materialises.
- ~~`asyncio_mode = "auto"` is loose — `pyproject.toml` silently auto-wraps any `async def test_*`. Switch to `"strict"` until async tests actually appear (Story 1.4+).~~ — **Resolved 2026-04-22 (Story 1.4 Task 6.2)**: `pyproject.toml` flipped to `asyncio_mode = "strict"`. Current test corpus has 0 async tests so no regression. Future async tests will now require an explicit `@pytest.mark.asyncio` decorator (catches the implicit-auto-wrap foot-gun).
- `athena.core.time` crashes at import if `tzdata` missing on Windows — `packages/athena-core/athena/core/time.py:11-12` eagerly constructs `ZoneInfo("Asia/Seoul")` at module import. Current dep `tzdata; sys_platform == 'win32'` guards the supported install path; only unsupported installs fail. Lazy-init `_KST` inside each function if the concern materialises.
- `detect-private-key` has no regression test — `.pre-commit-config.yaml` declares the hook but supply-chain "no secrets" NFR-S1 has zero fire-drill coverage. Add a test that commits a PEM fixture and asserts the hook rejects, during Story 1.3 CI hardening.
- `.importlinter` layers `|` sibling syntax is fragile — `.importlinter:113` requires literal `" | "` with spaces around the pipe; human-editor mistakes silently degrade contract. Add an inline explanatory comment when Story 1.2+ next touches the config.
- `COMMIT_RE` in test_hatch_hook.py under-anchored — `packages/athena-core/tests/test_hatch_hook.py:20` has no trailing `\s*$`, so would capture garbage after the closing quote if the hook's generated file format ever changes. Correct today, fragile for future edits.
- `test_frozen_prevents_mutation` relies on Pydantic patch-version semantics — `packages/athena-core/tests/test_dto.py:579-582` catches only `ValidationError`; historically frozen mutation raised `TypeError`. Widen to `(ValidationError, TypeError)` if Pydantic behaviour shifts.
- `tests/regression/test_ruff_bans.py` matches bare `"TID"` not `"TID253"` — resilient to ruff rule renames but the intent is undocumented. Add an inline comment explaining the choice.

## Deferred from: code review of 1-2-환경-secrets-infrastructure-wsl2-os-keychain-ssh-signing (2026-04-22)

- Runtime `_EXCLUDE_DIRS` missing tool-cache dirs (`.mypy_cache`, `.ruff_cache`, `.pytest_cache`, `.import_linter_cache`) — `packages/athena-core/athena/core/settings.py:45-56`. Regression walker already excludes them; runtime depth-1 guard could theoretically trip if a tool writes `.env` into a cache dir. AC-3 enumerates 8 dirs explicitly so widening is spec-expansion; cache-dir `.env` is extremely unlikely in practice. Revisit if ever observed.
- Windows junction point (`mklink /J`) behavior vs POSIX symlink in regression walker — `tests/regression/test_no_dotenv_files.py:62-63`. `pathlib.Path.is_symlink()` has shifted across Python versions for junctions; blanket `if entry.is_symlink(): continue` could silently hide `.env` at a junction target. Platform quirk requiring cross-platform CI (currently single-host Windows/WSL2). Revisit during Story 1.3 CI hardening when matrix expansion is discussed.
- Review commit `439df6f` (`test(story-1.2): apply code-review patches`) landed **unsigned** because Windows host `~/.gitconfig` has no SSH signing configured. Per Story 1.2 Task 5.7, Windows-side signing was explicitly deferred to Story 1.7. Story 1.7 must (a) set up Windows host `gpg.format=ssh` + `user.signingkey` matching WSL2 side, and (b) decide policy for the unsigned gap (`439df6f` + any future Windows-host commits before Story 1.7 completes) — NFR-A5 audit chain has a known discontinuity at this SHA. Do NOT amend `439df6f` to retroactively sign; history rewrite is banned (Dev Notes invariant 6).

## Deferred from: Story 1.3 (2026-04-22)

- Paper replay marker auto-generation workflow — `scripts/check_paper_replay_marker.py` only inspects whether `paper-replay-ok/<short_sha>` exists. Tag creation remains manual (see operating_playbook § "Story 1.3 Task 4.6 — Paper Replay Marker") until Epic 8 Story 8.5 implements the paper-replay gate workflow that publishes the tag on successful paper re-run.
- Snapshot regression real fixture — CI stage-4 skips via `SNAPSHOT_FIXTURE_MISSING`. Epic 2 Story 2.1 AC-4 must remove that skip marker and populate the historical-failure S_entry fixture.
- Walk-forward smoke implementation — CI stage-5 skips via `WALK_FORWARD_RUNNER_NOT_IMPLEMENTED`. Epic 8 Story 8.3 implements `scripts/walk_forward_runner.py` and replaces the skip with a 2-3 trial smoke run.
- Branch protection drift auto-detection — `infra/github/branch_protection.json` baseline lands after Khuk0 runs `scripts/setup_branch_protection.sh`. Monthly CI job diffing the baseline vs live config is deferred to Story 1.9 (observability) or Epic 8 Story 8.6 (policy change audit log).
- Policy-prefix-guard bypass hardening — the commit-msg hook is skipped by `git commit --no-verify`. Adversarial bypass is covered by CI stage-6 cooling gate, but an adjunct "lint-policy-commits" stage that parses the PR title for `policy:` could strengthen the developer-safety line (Story 1.9 optional add-on).
- Windows host (Logger PC) git SSH signing — Story 1.2 Task 5.7 deferred this to Story 1.7. Until then every commit must originate from WSL2. Branch protection `required_signatures: true` is enforced once Khuk0 runs Task 5, so Windows-host commits will be rejected at push time — Story 1.7 is now a hard blocker for any Windows-side git workflow.
- Runner auto-upgrade cadence — GitHub Actions runner bumps via `./config.sh --replace`. Auto-upgrade scheduling is bundled with Story 1.10 (backup automation) so a single systemd timer owns all operational cron entries.
- ~~Story 1.3 Task 1 / Task 5 host-setup artifacts~~ — **Resolved 2026-04-22**: Runner `athena-trading-pc` registered + systemd Active(running); `gh auth login --web` + `scripts/setup_branch_protection.sh` applied (strict config preserved); PR #1 + PR #4 both 7 stages green on self-hosted runner (`24763894291`, `24767278380`). sprint-status `1-3-*` → `review`.
- ~~Story 1.3 Task 6.6 end-to-end policy smoke~~ — **Resolved 2026-04-22**: AC-6 절차 1-7 전수 검증 완료. 3개 follow-up PR 로 수행: (1) PR #6 `story-1.3/policy-smoke-test` (closed, cooling-gate block evidence — run 24768772699 에서 72h window 내 policy commit 이 `POLICY_NOT_COOLED` 로 차단); (2) PR #7 `story-1.3/ci-ref-fix` (merged `2ea0770`, smoke 가 잡아낸 ci.yml bug 수정 — `actions/checkout@v4` default `refs/pull/N/merge` 로 stage-6/7 commit subject 검사가 빗나가던 문제); (3) PR #8 `story-1.3/stage7-smoke` (closed, stage-7 path evidence — run 24769144994 에서 genesis cooling pass / `PAPER_REPLAY_MISSING` fail, lightweight tag 생성 후 re-run 7/7 green). Smoke evidence 는 master 에 policy-chain 남기지 않도록 두 smoke PR 모두 merge 없이 close + tag cleanup.
- ~~Story 1.3 Task 5.5 protected-branch rejection capture~~ — **Resolved 2026-04-22**: 임시 branch `tmp/test-master-rejection` 에 empty commit 생성 후 `git push origin HEAD:master` 시도 → GitHub 응답 `GH006: Protected branch update failed for refs/heads/master. - Changes must be made through a pull request. - 7 of 7 required status checks are expected. ! [remote rejected] HEAD -> master (protected branch hook declined)` 캡처. temp branch 즉시 삭제.
- ~~Story 1.3 Task 1.6 WSL2 reboot auto-restart~~ — **Resolved 2026-04-22**: Khuk0 가 Windows PowerShell 에서 `wsl --shutdown` 실행 후 WSL2 재진입 → systemd system-level service 가 **4초** 만에 `Active (running) since 2026-04-22 17:59:44 KST` + `√ Connected to GitHub` + `Listening for Jobs`. GitHub API 쪽도 `{"busy":false,"name":"athena-trading-pc","status":"online"}` 자동 복귀. 부수 발견: runner 가 2.322.0 → 2.333.1 자체 self-update.
- Story 1.3 Task 5.6 Windows-host unsigned-commit rejection capture — Windows 가 commit 주체 아닌 상태에서는 재현 환경 부재. Story 1.7 Windows SSH signing 완료 후 재현 가능. `required_signatures=true` enforcement 자체는 PR #4 merge 시점에 verify 확인됨 (WSL2 signed commits `"verified": true, reason: "valid"`). deferred to Story 1.7.
- ~~Story 1.2 AC-4 GitHub signing SSH key upload~~ — **Resolved 2026-04-22 mid-Story-1.3**: Khuk0's WSL2 signing public key (`~/.ssh/id_ed25519_athena_sign.pub`) uploaded to GitHub account `ha-nyang-95` via the SSH-and-GPG-keys settings page as **Signing Key** (not Authentication). Previously only the `allowed_signers` local file was configured (local `git log --show-signature` worked) but GitHub-side verification (needed for `required_signatures: true`) had no key to resolve, so every WSL2 commit showed `"verified": false, reason: "unknown_key"`. After upload, all commits from the same key re-evaluate as `"verified": true, reason: "valid"` retroactively. Story 1.2 Change Log v1.3.0 records the gap.

## Deferred from: code review of 1-3-self-hosted-ci-cd-pipeline-7단계-gate (2026-04-22)

- `scripts/check_cooling.py:66` — negative or zero `%ct` (e.g. via `GIT_COMMITTER_DATE=@0`) makes `_now_utc() - prev_ts` a ~50-year delta that always clears `COOLING_WINDOW`; the cooling gate exits 0. Exploit requires write access to author the spoofed commit, so the threat model collapses to the holistic "can I trust timestamps in the commit ledger" question handled by Epic 6 (SHA-256 chain audit of the ledger against a signed external anchor). Deferred until Epic 6 Story 6.2 lands the chain-hash verification.

## Deferred from: Story 1.4 (2026-04-22)

- **Logger PC host setup** (SSH key registration, rsync server availability, Windows 11 + NSSM service wiring) — Story 1.7 prerequisite. Task 3.6 sub-step skipped in 1.4 because Logger PC is absent at W1 Day 1; `install_logger_sync_unit.sh` DRY_RUN=1 mode is the substrate, actual `enable --now` happens during 1.7.
- **Logger PC hourly export trigger** (NSSM service or Windows Task Scheduler running `scripts/export_parquet_shard.py --hour now-1`) — Story 1.7. Playbook cron expression `0 1 * * * *` (UTC) documented; actual scheduler install is 1.7.
- **Prometheus stack installation** (Prometheus binary, node_exporter textfile collector, Alertmanager routing) — Story 1.9. Story 1.4 ships only the rule file + emitter script; the rule cannot fire until 1.9 loads it.
- **rsync `ExecStartPost` duration measurement accuracy** — Story 1.4 emits `duration=0` (placeholder) because systemd does not auto-export start timestamp. Story 1.9 boosts this with `ExecStartPre=/bin/sh -c 'date +%s > /run/athena-sync-start'` + `ExecStartPost` reads the file to compute wall-clock duration. Affects `athena_logger_sync_duration_seconds` gauge only (last_success / last_exit_code already accurate).
- **DuckDB hot 7일 → Cold Parquet rollover** (`packages/athena-feature-store/athena/feature_store/retention.py` per architecture.md line 707) — Story 1.10 (backup automation). Story 1.4 keeps hot DuckDB growing unbounded and writes Parquet shards immediately per hour; the "hot 7 days" concept requires pruning the DuckDB tables of rows older than 7 days after the shard is confirmed rsync'd. 1.10 adds the confirmation signal + prune job.
- **Polars/DuckDB type stubs** — Both libraries ship typed APIs but rely on generated stubs that occasionally lag. If mypy starts failing on a new release bump, we either pin the library version or add a minimal `.pyi` override. No action required unless mypy breaks.
- **Trading PC write-scope ruff custom rule** (replaces the grep-style regression test) — Story 1.9. Current `test_trading_pc_write_scope.py` is MVP; ruff's custom rule API gives earlier feedback (at save-time inside the editor) and integrates with the existing `[tool.ruff]` config.
- **`_bmad-output/implementation-artifacts/1-3-*.md` status flip artifacts** — Story 1.3 file's `Status: in-progress → done` header update sits uncommitted alongside 1.4 (leftover from 1.3 review-flip session). Expected to flush with Story 1.4 handoff commit so the master tree converges.

## Deferred from: code review of 1-4-duckdb-parquet-shard-rsync-data-pipeline (2026-04-22)

- Symbol-less future storage table scenario — `export_hour_shard` has a `.select("symbol")` path that would `KeyError` on a hypothetical future storage table without a `symbol` column. V1.0 scope locks the three tables (`ticks`/`quotes`/`news`); revisit when any Epic 2+ story adds a new DuckDB storage table (e.g. modules_output columns grow) so the partitioning branch is generalized.
- SSH key / DNS availability at systemd `OnBootSec=30s` — `athena-logger-sync.service` `After=network-online.target` + `Wants=network-online.target` does not guarantee that `~/.ssh/id_ed25519_athena_sync` is mounted or `logger-pc` hostname resolves at first timer fire. First 1-2 rsync attempts may exit non-zero and emit `last_exit_code=23` (partial transfer) until the next iteration succeeds. `SuccessExitStatus=0 23 24 30` already tolerates this. Real root-cause (pre-register Logger PC in `~/.ssh/known_hosts` + verify key path) lands in Story 1.7 Logger PC host setup.
- NTP step-backward edge case — `scripts/emit_logger_sync_metric.py` uses `time.time()` directly. If NTP steps the wall clock backward after a successful run, `last_success_seconds > time()` and the Prometheus expression `time() - last_success > 120` becomes negative → alarm never fires during the skew window. Mitigation is a `now = max(int(time.time()), prev_last_success)` clamp, but the true root-cause is outside Athena (NTP policy on Trading PC). Revisit if we observe this in practice or when Story 1.10 adds `ntpd`/`chrony` configuration to the ops substrate.
- systemd unit absolute-path hardcoding — `infra/systemd/athena-logger-sync.service` inlines `/home/khuk0/invest_training/.venv/bin/python` + `/home/khuk0/invest_training/scripts/emit_logger_sync_metric.py`. Single-host/UID assumption is valid for V1.0 (Khuk0 personal system). Root-cause fix is an installer template pattern: `infra/systemd/athena-logger-sync.service.in` with `${ATHENA_HOME}`/`${ATHENA_USER}` placeholders that the installer (or its Story 1.7 Logger PC equivalent) renders via `envsubst` into `/etc/systemd/system/`. Also lets Story 1.7's NSSM-equivalent on Windows reuse the same template. Deferred to Story 1.7 when Logger PC installation patterns are designed end-to-end.

## Deferred from: Story 1.5 (2026-04-23)

- **외장 SSD 실 LUKS 초기화 + 실 디스크 조작** — `scripts/init_external_backup.sh` 는 `DRY_RUN=1` 까지 검증 완료 (4 dry-run 시나리오 pass). 실 하드웨어 연결 + `DEVICE=/dev/sdb1` 실행은 Story 1.10 (backup automation) 선행 + Khuk0 manual. `infra/systemd/mnt-external.mount` 파일은 git tracked 되었으나 `sudo systemctl enable --now` 은 Story 1.10.
- **S3 (또는 Naver Cloud) bucket 실 생성 + credential 주입** — `scripts/init_s3_object_lock.py --dry-run` + moto 5.x 로 Object Lock Compliance round-trip 검증 완료. 실 bucket 생성은 Story 1.10 / 6.2 가 실 credential (OS Keychain: `S3_ACCESS_KEY_ID` / `S3_SECRET_ACCESS_KEY`) 주입 후 수행.
- **`monthly-ledger-verify.yml` cron 미세조정** — 현재 `0 20 1 * *` (매월 2일 05:00 KST). 03:00 KST 가 self-hosted runner idle 시간대 최적이나 가용성 우선 05:00 KST. Story 1.10 이 실측 runner 가용성 기반으로 재튜닝.
- **`infra/prometheus/rules/ledger_integrity.rules.yml` 작성** — architecture.md line 828 에 파일명 명시. `verify_ledger.py` 의 exit code + stdout JSON `verdict` contract 를 consume. Story 1.9 Prometheus stack 설치 시 rule 파일 추가.
- **Global CB (Epic 5) 로 체인 불일치 트리거 연동** — `verify_ledger.py` 의 `"verdict": "CHAIN_BROKEN"` stdout 이 consume contract. 실 state machine 트리거는 Story 5.6.
- **Full `LedgerWriter.append` + event_type 전체 집합 확장** — V1.0 `LedgerEventTypeV1 = Literal["genesis", "schema_segment_transition"]` 2개뿐. Story 6.1 이 `entry_authorized / entry_rejected_* / order_placed / order_filled / exit_* / compliance_*` 로 확장 시 `tests/regression/test_ledger_event_type_literal.py` 의 BAD fixture 도 수정 필요 (현재 `"entry_authorized"` 사용 → Story 6.1 확장 후 out-of-set 값으로 교체).
- **monthly_ledger_chain.py 실 S3 업로드 (boto3 + per-object Object Lock retention)** — Story 6.2. 현재 `--s3-placeholder` 경로는 로컬 mirror 디렉토리.
- **anti_ego_events SHA-256 체인 재사용 결정** — Story 3.1 이 `athena.execution.ledger.hash_chain` 을 import 재사용 vs `athena-core` 로 승격 — Story 3.1 시점에 package 경계 재평가.
- **DuckDB row-level trigger 부재의 ruff 영구화** — `test_trading_pc_write_scope.py` 가 AST 레벨로 `INSERT INTO pre_trade_ledger_raw` 를 LedgerClient 외 파일에서 금지하고 있음. Story 1.9 가 동일 규칙을 ruff custom rule 로 승격해 editor-time 피드백 확보.
- **concurrent multi-connection append** — V1.1+ scope. 현 V1.0 은 단일 asyncio 프로세스 + 단일 DuckDB connection 가정 (architecture.md D13). 멀티 커넥션 append 경합은 `tests/integration/test_ledger_concurrency_smoke.py::test_multi_connection_append_is_deferred` 가 explicit skip 으로 scope 명시.
- **retention_days 윤년 오차** — V1.0 은 `5 × 365 = 1825` 일 고정 (윤년 무시). 실제 5년은 1826 또는 1827 일이므로 최대 2일 retention 단축. Story 6.2 가 per-object 설정으로 정확화 검토.
- **`athena_ledger_chain_valid` Prometheus gauge** — architecture.md line 430 에 네이밍 명시. rule 정의 + 메트릭 emit 은 Story 1.9.
- **3-way 검증 (외장 SSD ↔ S3 ↔ DB 재계산)** — 본 스토리는 1-way 재계산 (DB 만). Story 6.2 가 외장 SSD JSON + S3 오브젝트 + DB 의 segment_hash 를 3방 비교.
- **LUKS passphrase rotation 정책** — 현 `SecretName.LUKS_PASSPHRASE` 는 생성 후 rotation 스케줄 없음. Story 1.10 or 별도 ops 스토리에서 rotation procedure 명문화.
- **`boto3` type stubs** — 현재 `scripts/init_s3_object_lock.py` 가 `# type: ignore[import-untyped]` 로 회피. `botocore-stubs` 는 dev-group 에 있으나 `boto3` 본체는 stubs 를 생성적으로 제공하지 않음 (`mypy-boto3-*` 패키지로 개별). V1.0 은 ignore 로 진행, 실 broker 어댑터 (Story 4.1) 나 backup automation (Story 1.10) 에서 `mypy-boto3-s3` 추가 검토.

## Deferred from: code review of 1-5-pre-trade-ledger-초기-세그먼트-sha-256-체인 (2026-04-23)

- **Chain tip `SELECT this_hash FROM pre_trade_ledger_raw ORDER BY id DESC LIMIT 1`** — `packages/athena-execution/athena/execution/ledger/client.py:113-114`. Gap/replay ID (외부 스크립트의 수동 insert, sequence reset+재진행, multi-writer race) 발생 시 가장 높은 `id` 를 chain tip 으로 오판. V1.0 single-writer asyncio 전제 하에서는 safe. 본 스토리의 `concurrent multi-connection append` defer 와 같은 root cause — V1.1+ 에서 `parent_id` 컬럼 도입 또는 `prev_hash IS NULL` 제너레시스부터의 topological traversal 로 교체.
- **DELETE 후 genesis 재시드 불가능** — `packages/athena-execution/athena/execution/ledger/schema.sql:30` 의 CHECK `(id = 1 AND prev_hash IS NULL)` + `client.py:67-99` 의 `nextval()` sequence 누적. `DELETE FROM pre_trade_ledger_raw` 후 새 `LedgerClient` 생성 시 id ≥ 2 로 genesis 시도 → CHECK 위반 raise. AR-DATA4 "새 스키마 = 새 DuckDB 파일 + 새 세그먼트" 가 design invariant 이나, threat model §#2 (DB 파일 삭제 후 재생성) 의 복구 시나리오에서 필요한 절차 — Story 6.2 3-way verify 설계 시 `ALTER SEQUENCE seq_pre_trade_ledger_id RESTART` + 외장 SSD segment_hash 로부터의 genesis 복원 procedure 를 공식화.
- **`_ensure_genesis` 명시적 트랜잭션 누락** — `packages/athena-execution/athena/execution/ledger/client.py:67-99`. DuckDB single-statement autocommit 에 의존. WAL checkpoint + process crash 조합의 corner case 에서 sequence 가 advance 되고 genesis row 가 rollback 되는 상황이 이론적 가능. V1.0 low-probability. Story 6.2 3-way verify 에서 `BEGIN; INSERT genesis; COMMIT;` 명시화 + sequence 복구 procedure 정립.
- **`monthly-ledger-verify.yml` continuity wiring** — `.github/workflows/monthly-ledger-verify.yml:28` 은 현재 `verify_ledger.py --db $DB_PATH` 만 호출하여 per-row `this_hash` 재계산만 수행. `--prev-segment-json / --year / --month` 옵션은 AC-6 narrative 의 "prev 월 segment_hash ↔ 현 월 genesis prev_hash 연속성" 을 실행할 수 있으나 실 `LEDGER_DB_PATH` 및 `monthly_ledger_chain.py` 월간 산출물 경로가 Trading PC 에서 확정되어야 정확히 계산 가능. Story 1.10 backup automation 의 real cron tuning + `LEDGER_DB_PATH` env wiring 과 함께 처리. 본 스토리는 substrate 수준 (per-row 재계산) 까지로 scope 명시.



- **`tests/integration/test_parquet_shard_export.py` 가 repo root 에 `--out-root` orphan DuckDB 파일 생성** — Story 1.4 review-flip (commit b25d80a) 에서 미발견. `pytest tests/integration/test_parquet_shard_export.py` 단독 실행 시 12,288 바이트 DuckDB 파일이 working-directory 루트에 반복 생성됨. 원인 후보: `_run_cli` 가 `subprocess.run(cwd=repo_root, ...)` 로 CLI 를 기동하는 경로 중 일부에서 `duckdb.connect("--out-root")` 가 호출되는 argparse/인자 순서 버그 또는 side-effect. 즉시 조치: `.gitignore` 에 `/--out-root` 패턴 추가하여 커밋 오염 차단. 근본 원인 수사는 Story 1.4 재개 세션 또는 별도 bugfix 스토리에서 수행. **Story 1.5 review-flip blocker 아님** — `.gitignore` 으로 블래스트 radius 차단.

## Gemini Code Assist bot 자동 리뷰 findings — PR #10 (Story 1.5 merge, 2026-04-23)

PR #10 merge 시 `required_conversation_resolution: true` branch-protection 해제를 위해 3개 review thread 를 resolve 처리했으나 **실 코드 수정은 수행하지 않음** — 지적 자체는 유효 (3건 모두 medium priority, performance/memory 범주) 하며 V1.0 scope 에서는 OOM/roundtrip 영향 미미하므로 follow-up bugfix 스토리 또는 Story 6.1 full LedgerWriter 구현 시점에 일괄 반영 권장. 원 thread ID: `PRRT_kwDOSGvmD858_9IO` / `858_9IW` / `858_9IZ`.

- **`packages/athena-execution/athena/execution/ledger/client.py:146-152` — `SELECT currval('seq_pre_trade_ledger_id')` 별도 query 대신 `INSERT ... RETURNING id` 사용 권장** — 1 roundtrip 감소 + session-local `currval` state 의존 제거 (V1.0 단일 asyncio 프로세스에서는 비관련, V1.1+ multi-connection 에서는 필수). DuckDB 1.x `RETURNING` 지원 확인 완료. **조치 후보**: `client.append` 의 마지막 `SELECT currval` 호출 삭제 + `INSERT ... RETURNING id` 의 `fetchone()` 으로 id 수령. Low-medium priority — Story 6.1 full LedgerWriter 가 append path 를 통째로 재작성할 때 같이 처리.
- **`packages/athena-execution/athena/execution/ledger/segment_hash.py:65-74` — `fetchall()` 대신 cursor iteration + shadowing 방지용 `i` 변수** — 월당 entries 가 많아질 경우 (V1.0 기준 월 수천 건, Story 6.1+ 로 확장 시 월 수만-수십만 건 가능) `fetchall()` 이 전체 결과를 메모리에 적재 → OOM 위험. 또한 `str(id)` 의 `id` 는 Python builtin 과 shadow. **조치 후보**: `for (eid,) in conn.execute(...):` 직접 iteration + `i` 또는 `eid` 로 rename. V1.0 low priority — verify_ledger 패턴과 함께 일괄 수정 권장.
- **`scripts/verify_ledger.py:39-76` — `rows = ... .fetchall()` 대신 cursor iteration** — Ledger 가 N 년간 성장하면 전체 체인이 수백만 건 ~ 수천만 건 될 수 있음. `fetchall` 은 모두 메모리 로드 → 검증 스크립트 OOM. **조치 후보**: `res = conn.execute(...); for row in res:` 로 iterator. 현재 `last_this` 상태 기계는 unchanged. Medium priority — Story 6.2 3-way verify 설계 시점에 필수 처리 (3-way 는 더 큰 메모리 범위). 본 스토리는 V1.0 scope 에서 bypass.

## Gemini Code Assist bot 자동 리뷰 findings — PR #11 (Story 1.4 merge, 2026-04-23)

PR #11 merge 시 `required_conversation_resolution: true` branch-protection 해제를 위해 4개 review thread 를 resolve 처리했으나 **실 코드 수정은 수행하지 않음** — 지적 자체는 유효하며 follow-up bugfix 스토리에서 처리 권장. 원 comment ID 들: `PRRT_kwDOSGvmD859AJER` / `859AJEV` / `859AJEg` / `859AJEk`.

- **`packages/athena-feature-store/athena/feature_store/duckdb_client.py:28` — DuckDBPyConnection context-manager docstring 오해 유도** — DuckDB 1.x 의 `DuckDBPyConnection` 은 `with` 블록 진입 시 트랜잭션만 관리하며 exit 시 자동 `close()` 안 함. 현 docstring 은 close 가 자동으로 되는 것처럼 읽힘. Windows 에서 미종료 connection 이 `.wal` 락 남기면 다음 연결 차단 위험. **조치 후보**: (a) `contextlib.closing(open_logger_duckdb(...))` 사용 유도 docstring 으로 수정, 또는 (b) 래퍼 함수를 `@contextmanager` 로 재설계. Medium priority. Story 4.3 orders pipeline 이 connection lifecycle 을 집중적으로 다룰 때 함께 감사 권장.
- **`packages/athena-feature-store/athena/feature_store/parquet_shard.py:206` — 심볼별 `df.filter()` 루프 O(N_symbols × N_rows)** — 현재 구현은 심볼 목록을 순회하며 DataFrame 전체를 매번 filter. 심볼 수 많아지면 (V1.0 portfolio 3종목 scope 에서는 실측 영향 없지만 V1.1+ 확장 시 병목 가능) 성능 저하. **조치 후보**: Polars 의 `df.partition_by("symbol")` 로 단일 pass 분할. Medium priority. Epic 5 Story 5.2 (3종목 동시 상한) 까지는 현재 구현으로 무방하나, symbol universe 확장 시 (Epic 5+) 재검토.
- **`packages/athena-feature-store/athena/feature_store/parquet_shard.py:233` — `mkdir` 루프 내 중복 호출** — `partition_dir` 은 한 함수 호출 동안 모든 심볼에 대해 동일한데 루프 내부에서 매번 `mkdir(exist_ok=True)` 호출. 마이너 시스템 콜 낭비. **조치 후보**: 루프 외부 (194행 부근) 로 이동. Low-medium priority. 위 206 line 수정과 함께 처리 권장 (같은 함수).
- **`scripts/emit_logger_sync_metric.py:56` — `line.split()` 결과 길이 미검증 → `IndexError` 가능성** — Prometheus textfile 포맷 파서가 파일 손상으로 접두사만 남은 경우 (예: `"last_success_seconds"` 만 있고 값 없음) `parts[1]` 접근이 `IndexError` 발생. 현재 Story 1.4 review-flip 에서 다룬 NTP step-backward + exit-code 23 handling 과는 별개 edge case. **조치 후보**: `parts = line.split(); if len(parts) >= 2: ...` 가드 추가. Medium priority — 실제 파일 손상 빈도는 낮지만 observability 파이프라인의 crash 이므로 우선순위 의외로 높음. 별도 single-commit bugfix 스토리 또는 Story 1.10 backup-automation 단계에서 logger_sync 전체 감사 시 포함 권장.

## Deferred from: Story 1.6 (F5 읽기전용 마운트 systemd Timer)

Story 1.6 은 F5 OS-layer tamper-resistance substrate — scaffold + contract + infra 까지만 책임진다. 아래 항목은 의도적으로 후속 스토리 몫:

- **`athena-inotify-watcher.service` ExecStart 실 구현 + `[Install]` 섹션 추가** → **Story 3.5**. 현 scaffold 는 `sleep infinity` placeholder (`systemd-analyze verify` 통과용). Story 3.5 가 `python -m athena.alpha_defense.f5.inotify_watcher` 로 교체 + `WantedBy=multi-user.target` 추가 + install.sh 에서 enable.
- **`anti_ego_events` 테이블 DDL + SHA-256 체인 + F5 OVERRIDE_ATTEMPT event append** → **Story 3.1 + Story 3.5**. 본 스토리는 `OverrideAttemptEvent` dataclass contract 만. 실제 `anti_ego_events` persist, `athena.execution.ledger.hash_chain` reuse, canonical JSON serialisation 은 Story 3.1.
- **`infra/prometheus/rules/readonly_mount.rules.yml` — Prometheus alert rule 파일** → **Story 1.9**. 예상 rules: `athena_readonly_mount_state{state="PARTIAL"} == 1 for 5m` → Medium; `time() - athena_readonly_mount_last_lock_success_timestamp_seconds > 86400` → Critical (24h 잠금 실패); `time() - athena_readonly_mount_last_transition_timestamp_seconds > 604800` → Medium (7일 미갱신). 본 스토리는 메트릭 emit + naming contract 까지만.
- **Alertmanager 라우팅 (PARTIAL / 잠금 실패 / unlock 실패 분류)** → **Story 1.9**. Prometheus rule 선행 필수.
- **`timedatectl set-timezone Asia/Seoul` 자동 설정** → **Story 1.10 host setup**. install.sh 는 warn 만 (destructive timedatectl 변경 지양). `OnCalendar=Mon..Fri 09:00` 이 KST 로 evaluate 되려면 WSL2 timezone 이 Asia/Seoul 이어야 함 — 현재는 운영자 수동.
- **`/etc/athena/extra_closed_days.txt` systemd credential 주입 방식** → **V1.1+**. 현 V1.0 은 plain text, 운영자 직접 편집. systemd credential 은 보안상 더 나은 방식이지만 V1.0 단일 운영자 scope 에서 overkill.
- **`config/` → `/var/lib/athena/policy/` 자동 재배포 (rsync 또는 git post-merge hook)** → **Story 1.10**. 현 V1.0 install.sh 는 1회 seed 후 덮어쓰지 않음 (운영자 수동 재배포 allow). Story 1.10 이 git post-merge hook + 15:30 unlock 창 활용 automated sync 설계.
- **`ReadonlyMountController.DEFAULT_PROTECTED_PATHS` 확장 절차** → **Story 6.5** (세후 수익률 경보). 확장 예: `tax_schedule.toml`. Change Control 3 step: (1) DEFAULT_PROTECTED_PATHS tuple 에 path 추가, (2) sudoers drop-in `+i`/`-i` 2 entry 추가, (3) install.sh 재실행. Invariant #1.
- **ruff custom rule: 직접 `subprocess.run(["chattr", ...])` 호출 금지 (AST 레벨 enforce)** → **Story 1.9**. 현 V1.0 은 Python 단일 진입점 (`SubprocessChattrExecutor` in `readonly_mount.py`) invariant 로만 enforce. 다량 위반 가능성 낮아 AST 강제는 optional.
- **KRX 임시 휴장 자동 fetch (OpenKRX API)** → **V1.1+ 또는 Story 1.10**. 현 V1.0 은 `--extra-closed-days-file` 수동 보강. OpenKRX API 호출은 네트워크 의존 → F5 의 "장중 불변성" 철학과 부분 충돌 (ExecStartPre 가 네트워크 대기하면 09:00 lock 이 지연).
- **systemd timer `Persistent=false` → `true` 재평가** → **Story 1.9 observability 데이터 확보 후 재검토**. 현 `false` 는 "missed fire 이 의도적 미수행" (운영자 놀라움 방지 — Invariant #6). 운영 데이터 축적 후 재판단.
- **sudoers drop-in 전체 감사 (본 스토리 외 NOPASSWD 정책 포함)** → **Story 6.6 / 6.7 (준법감시인 워크플로우)**. 월간 감사에서 sudoers diff 를 외부 승인권자에게 보고.
- **장중 override 시도 실제 테스트 (사용자가 의도적 git revert 시도하는 적대적 시나리오)** → **Story 3.5 + Story 3.7 통합 테스트**. 본 스토리는 `test_chattr_e2e.py::test_git_revert_blocked_during_lock` 까지 — cp 수준 primitive 검증. 실제 운영자가 git revert 시도하는 시나리오는 Story 3.5 watcher 가 OVERRIDE_ATTEMPT event 를 emit 하는 통합 테스트에서 cover.
- **Systemd `User=root` vs `User=khuk0 + sudoers` 재평가** → **Story 1.9 관측 후**. 현 디자인 이유는 Invariant #8 (audit trail + sudo boundary 최소화). tradeoff 는 운영 데이터 축적 후 판단.
